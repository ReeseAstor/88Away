# Database Configuration
# Replace this with your actual Neon database URL
# Format: postgresql://username:password@hostname/database?sslmode=require
DATABASE_URL="postgresql://user:password@ep-example-12345.us-east-1.aws.neon.tech/dbname?sslmode=require"

# Replit Authentication Configuration
# Replace with your actual Replit domain(s)
REPLIT_DOMAINS="localhost"
# Replace with your actual Replit app ID
REPL_ID="debug-repl-id"
# Replace with a secure session secret
SESSION_SECRET="debug-session-secret-key-for-development-only"